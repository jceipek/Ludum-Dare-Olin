from manager import Dimension

METER = Dimension(value=1.0, units={'m': 1})
PIXEL = Dimension(value=1.0, units={'px': 1})

PIXELS_PER_METER = (METER / PIXEL)
PIXELS_PER_METER.Canonicalize()
PIXELS_PER_METER = PIXELS_PER_METER.value

SCREEN_PIXEL_WIDTH = 1024
SCREEN_PIXEL_HEIGHT = 768
SCREEN_REAL_WIDTH = SCREEN_PIXEL_WIDTH / PIXELS_PER_METER
SCREEN_REAL_HEIGHT = SCREEN_PIXEL_HEIGHT / PIXELS_PER_METER

GRAVITY = (0,-10) # [m s^-2]
AIR_RESISTANCE = 0.1 
FRICTION = 0.2
FPS = 60 # [s^-1]
DENSITY = 0.4

MAX_WALK_SPEED = 1 # [m s^-1]
